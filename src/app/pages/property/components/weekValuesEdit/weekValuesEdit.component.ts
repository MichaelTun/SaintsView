import { Component, OnInit } from '@angular/core';
import { FormGroup, FormControl, Validators, FormBuilder } from '@angular/forms';
import { NgModule } from '@angular/core';

@Component({
    selector: 'weekValuesEditComponent',
    templateUrl: 'weekValuesEdit.component.html'
})
export class WeekValuesEditComponent implements OnInit {
    constructor() { }

    ngOnInit() { }
}